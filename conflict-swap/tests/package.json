{
  "name": "conflict-swap-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright test suite for conflict-swap.html, run in isolation from the main YooBees app.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0"
  }
}
